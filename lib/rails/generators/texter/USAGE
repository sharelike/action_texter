Description:
============
    Stubs out a new texter and its views. Passes the texter name, either
    CamelCased or under_scored, and an optional list of messages as arguments.

    This generates a texter class in app/texters and invokes your template engine.

Example:
========
    rails generate texter Notifications signup forgot_password invoice

    creates a Notifications texter class, views, and test:
        Texter:     app/texters/notifications_texter.rb
        Views:      app/views/notifications_texter/signup.text.erb [...]

